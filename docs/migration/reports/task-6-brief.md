# Task 6: scoped draft generation, durable provenance and review UI

Worktree: C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/outputs/softr-job-board. Execute PR6 of the attached plan; PR1-5 must be reviewed before dispatch. User corrections override the plan: fresh Better Auth email/password accounts, no Auth0 and no legacy account migration/claiming. Do not add account-linking code. You own generation service/domain/schema, application request/status UI/API, GenerateDraftWorkflow/provider adapter in the existing background Worker, focused tests/evaluation fixtures and generation runbook. Add 0006_generation.sql; never alter already-applied0001-0005. Integrate root configurations where required. No subagents, Git staging/commits/push, real mail, live provider calls, secret reads, source workflow execution, provisioning or deployment. Do not read protected source archives. Public synthetic test credentials are in tests/e2e/synthetic-accounts.mjs. Keep the stable localhost8787 preview untouched; use a fresh validated SYNTHETIC_PORT if a browser preview is needed.

Use the current shared owner-aware UserFlowService/DataService, strict Zod inputs, immutable profile/job/draft versions, composite owner foreign keys, private R2 and native D1 transactional batches. All remote resources remain unprovisioned and all release flags off. No Queues, Agents/DO framework, Workers AI or provider replacement, automatic submission or unconstrained agent tools. Read the relevant Workers/Workflows skill/types before their operations and current installed code. Use meaningful red-to-green tests in real Workerd/D1 for transactional/concurrency contracts, not only mocked DB chains.

## Request and immutable input contract

An authenticated action names exactly one owned saved job and chosen owned profile VERSION (do not use first profile or implicit fallback), optional owned immutable resume reference, a current job-content VERSION, an idempotency key, and an expected target draft revision when regenerating. Missing/wrong-owner/archived profiles, mismatched saved-job/draft ownership, cross-owner files, inconsistent profile-version/resume-parent relationships and unsupported legacy profile content fail closed. Check owner predicates again in the Workflow. Never scan all pending drafts from a browser request.

Commit exactly one generation request, durable immutable input references/content hash, outbox event and attributable audit together. A repeated owner/idempotency key with identical intent returns that request; a changed payload conflicts. Bound per-user accepted requests and in-flight requests using durable, race-safe limits, with documented conservative configurable defaults. A request creates a missing draft when appropriate; never require clearing a cover letter. Duplicate concurrent requests cannot create extra drafts or outbox effects. Model configuration is server-controlled, not request-controlled. Return accepted/queued, never completed at request time.

Preserve the last good draft while pending or on failure. An explicit regeneration produces a new immutable version only if its expected draft revision is still current and unchanged by edit/approval; never silently overwrite an approved revision or user edits. Choose and document a coherent request lifecycle that allows old content to remain reviewable, with optimistic version guards on final commit. The existing DataService.editDraft rejects queued/running draft execution status; adjust only as needed to preserve safe last-good editing and meaningful race tests. Do not block all user editing merely to avoid the required concurrency contract. Saved-job summary changes and draft/request/audit commits must be consistent, must not clobber concurrently edited notes/status, and must respect submitted/archived outcomes. Approval remains an explicit separate action with expectedRevision.

Input snapshots belong in private immutable R2 keys with D1 manifest/hash. Keep large text out of Workflow step results. D1/R2 are not one transaction: record recoverable snapshot states, validate bytes/hash before using a snapshot, prevent dangling successful requests, and make replay safe. Source/job content is untrusted text and may contain instructions; it must never change endpoint, model, tool availability or authorization. Private snapshots/provider responses stay out of logs/public fixtures.

Build provider input from explicit allowlisted job fields and the caller's selected profile evidence. Retained legacy job-version archives may include source relationship metadata (for example Recommended For IDs); raw source archives/rosters and unrelated ownership metadata must never be forwarded to Anthropic or returned through generation status. Input-version provenance does not require exposing all archived source fields.

Optional resume: current uploads and imported files are quarantined. Only available, clean, owned, checksum/size-matching bytes may be used. A selected file alone is not parsed candidate evidence. Do not add an untested PDF text parser; either implement a bounded, explicit tested extraction stage or reject unsupported resume use with a clear actionable message while allowing generation from explicitly chosen profile evidence. Persist whether resume contents were actually used. Do not send private data to a provider during this implementation task.

## Durable dispatch and Anthropic adapter

Extend the existing background Worker with GenerateDraftWorkflow and a generation-outbox dispatcher. Reuse proven deterministic Workflow ID/status handling from ingestion where appropriate; D1 is the indefinite deduplication ledger. A browser never directly invokes Anthropic. No new random instance after ambiguous create response. Bound dispatch retries and expose truthful failure/attempt state. Independent app request and background execution kill switches remain false by default; stopping them prevents new provider side effects without erasing queued evidence.

Generation requests need prompt dispatch: the existing hourly ingestion cron alone would make a request wait up to an hour. Use a distinct frequent bounded outbox dispatcher (for example minute cron, ingestion acquisition still evaluated once per logical date) or a private Worker service-binding kick with a scheduled durable fallback. Never expose an unauthenticated public kick endpoint or let application code issue untracked model calls. Preserve hourly ingestion semantics and five-source independence. Cover the ingestion Task5 abandoned-final-lease fix when reusing dispatcher patterns.

Provider remains Anthropic. Source Softr alias anthropic-claude-opus-5 is NOT the direct API model ID. Current official docs explicitly show claude-opus-5 (https://platform.claude.com/docs/en/build-with-claude/structured-outputs); model access is not verified because ANTHROPIC_API_KEY is absent. Do not call a live provider or infer account access. Document an operator model-access preflight using the Models API; deploy enablement still requires that check and reviewed evaluation. Endpoint is fixed api.anthropic.com/v1/messages with secret binding, bounded timeout/input/output bytes/token budget and no redirects/arbitrary caller endpoint. Use current official docs for exact API header/parameter compatibility. Structured output uses output_config.format JSON schema, with local strict constraints as well; refusal and max_tokens are failures even if HTTP200. Do not treat JSON shape as factual grounding.

Record each attempt before the external call, with bounded maximum attempts, timestamps, redacted error classification, provider request/message ID when available, exact model, usage and finish reason. An ambiguous timeout after acceptance must not silently run another billable request during Workflow step retry: durable attempt state must stop or require an explicit retry disposition. Exactly-once provider billing cannot be promised. Keep definitive retryable failures distinct from unknown acceptance. Successful provider response persistence must precede DB finalization so retries can reuse it. Strictly validate empty, oversized, malformed, truncated, refused, wrong-model and unsupported outputs. No unlimited error-driven token escalation.

## Evidence and output contract

Derive deterministic gap questions from the selected profile evidence without inventing candidate shortcomings: unknown/missing evidence means a question, not a claim that the candidate lacks a skill. Preserve user-supplied profile claims as user-provided evidence, not externally verified facts. Require generated factual assertions to reference exact supplied evidence; choose a conservative bounded strategy and explain its limits. A reference ID alone does not prove a free-form sentence follows that evidence. Reject or visibly require review for unsupported facts, numerical/employer/credential assertions, or invented evidence. Do not claim blanket hallucination-free output. Keep listing facts distinct from candidate facts and distrust prompt injection. No secret/system-prompt disclosure, URLs fetched from text, or tool execution.

Output should include a reviewable cover letter, optionally clearly labeled inferred screening questions/answers, evidence references and gap questions. Do not present inferred questions as actual employer questions. Preserve complete provenance: actor/request, saved job/job/profile/resume versions and hashes, model ID, prompt/output contract version, provider IDs, start/end, usage, validation outcome, and immutable draft revision. Render provider text as text, not executable HTML. Human approval never submits externally.

## User experience and focused acceptance

Keep existing presentation. Saved details exposes chosen profile/version and optional resume state, request/regenerate action only when server readiness permits, pending/failed/ambiguous status and the last good content, evidence/gaps/review information and explicit approval. Status must survive reload. On conflicts retain unsaved form values and offer a safe refresh/recovery. Generation unavailable remains an honest visible state while deployment/evaluation flags are off; do not turn it on in defaults just to show the UI. Local test harness may explicitly enable mocked provider behavior only in isolated tests, never production bypass routes or credentials. Ensure stale saved-form revisions can be recovered after request status updates without silently clobbering edits.

Required focused tests: wrong/missing owner profile; multiple profiles and explicit selection; mismatched save/draft/profile/resume; absence of evidence; immutable snapshot/hash; injection listing; empty/refused/truncated/malformed/unsupported output; timeout after provider acceptance; duplicate same/different intent; Workflow restart/duplicate finalization; user edit while running; approval/regeneration race; rollback preserves last good draft; kill switches, per-user durable limits; truthful API/UI pending/failure and no automatic submission. Test real Workflow wrapper with mocked network and native D1/R2. Add synthetic evaluation fixtures with expected supported/unsupported claims and review outcomes; these are deterministic contract tests, not proof of live model quality. At least a focused browser test should cover request state and retained edits if feasible with the existing harness; otherwise name exactly what remains unverified.

Deliver .superpowers/sdd/softr-cloudflare-migration-plan-2026-09-11/task-6-report.md with changed file list, commands/results, TDD evidence, contract/design decisions, metrics and limitations. Run relevant lint, all affected typechecks and both Worker dry-run builds, plus focused tests and necessary regression coverage once. Record missing key/access/human evaluation as gates without pretending production generation is ready. Stop for parent scoped review after implementation; do not begin MCP.
