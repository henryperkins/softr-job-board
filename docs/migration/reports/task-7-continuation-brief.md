# Phase 7 continuation

User now explicitly requests: "Finish Phase 7 and move on." This supersedes the earlier stop-before-7 boundary. Finish this implementation unit, report for independent review, and let the controller handle Phase 8. Read task-7-brief.md first for exact requirements, then current code. This brief adds current context and ownership limits; it is not a request to rebuild completed phases.

Worktree: C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/outputs/softr-job-board. Branch codex/cloudflare-migration; reviewed Phase 1–6 index tree 64f93b5d350664320a8c1f3d0c977f337b1cd7d4; base 95799ea261871402be2598f5aea3d4109f6065f2. Preserve all staged and unstaged work. No staging, commits, push, deployment, cloud resources, paid calls or real email. No subagents. Do not alter migrations 0001–0006. Parent owns reports outside repo, ledger and Phase 8 preparation. You own remaining Phase 7 documentation, focused browser acceptance, release-evidence template, necessary bounded fixes and task-7-report.md.

Current existing Phase 7 implementation was provided with a user checkpoint: opaque tokens via disableJwtPlugin:true, internal-only companion endpoint calling requireActiveAccessToken; immediate revocation and disconnect removing grant tokens; re-consent does not revive tokens. User reported 40 unit, 61 integration, 62 jobs (25 ingestion + 37 generation), lint/typechecks across four projects and all dry builds passing. These are supplied results, not a substitute for your focused verification of changes. Core files include workers/app/src/mcp, packages/domain/src/mcp.ts, 0007_mcp.sql, consent/account UI, scripts/ops, observability and native tests. Inspect current implementation and retain it. The recorded remaining work is connection docs + README, real browser consent/revocation acceptance, release-evidence template, report and independent review. You finish everything before the independent review, which the controller will dispatch separately.

User checkpoint reported production audit 224 dependencies/zero advisories and one pre-existing dev-only sharp advisory via vitest-pool-workers/miniflare. Verify provenance if documenting it; do not update unrelated dependency stacks without a task-specific need. Preserve security regressions for cross-origin business/auth/consent POSTs and non-HTML unknown protocol routes. Read exact current providers' primary docs only as needed for client connection examples; for Codex inspect local CLI first and official OpenAI docs as fallback.

Browser acceptance must exercise actual synthetic login -> authoritative consent allow/deny -> PKCE exchange -> MCP call -> Account disconnect -> next MCP call rejection, plus re-consent without old-token resurrection as feasible. Reuse existing synthetic preview machinery with a new port (8899 suggested; check free first); do not touch stable localhost8787. Capture useful passing UI evidence. No real accounts, emails or external providers. Run focused tests while iterating; at completion run relevant Phase 7 native tests, affected lint/types and builds once. Parent will use your actual report rather than rerun identical suites. Read applicable frontend testing and verification skills. Use TDD for fixes where behavior changes.

Write full report at .superpowers/sdd/softr-cloudflare-migration-plan-2026-09-11/task-7-report.md, including current complete Phase 7 changed-file list relative to Phase 1–6 index, design, commands/counts, user-reported vs newly run evidence, browser outcome, limitations, security/ops interfaces and self-review. Return only status, one-line tests, concerns and report path. Do not independently review yourself via another agent; controller performs review. Escalate concrete architecture gaps rather than invent a new scope.
