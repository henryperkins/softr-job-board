# Task6 scoped fix round1

Read task-6-review.md. Fix exactly its two P2 findings in the existing reviewed design:

1. First-time generation creates a queued generation-provenance draft. Allow safe pending edits in the UI consistently with the shared service's provenance exception, without weakening legacy execution/approval restrictions. Add a real browser regression starting with a saved job that has no draft: request, type, save while queued, reload and verify persistence. Preserve the existing native finalization guard proving a later generation cannot overwrite an edited/approved revision; extend only if the new scenario exposes a gap.
2. Prevent an accepted request whose evidence cannot satisfy the renderer's6,000-character limit. Use the existing whole-field evidence contract, not a new segmentation feature. Ensure provider-selectable evidence obeys that contract and reject zero eligible evidence with an actionable profile-input error before durable acceptance, quota consumption, outbox/draft mutation or provider call. Cover6,000/6,001 boundaries and, if filtering is used, mixed eligible/ineligible evidence without silent truncation. Keep immutable source-version/hash provenance and strict output validation.

Run focused red-to-green native/browser regressions and affected lint/typechecks; update docs/generation.md only for the precise evidence limit/action. Current native suite35 and integration31 alreadypassed, so broaden only as these changes warrant. Preserve stablelocalhost8787; use fresh isolated synthetic port. No live provider/mail/scans/private archive reads/MCP/provisioning/deployment, no subagents or Git staging/commits. Parent staged the26-file pre-fix Task6 snapshot, so your changes form a clean scoped diff. Write task-6-fix1-report.md with paths/commands/results/limits, then stop for re-review. Do not begin Task7.
