# Phase 7 scoped fix round 1

The independent task review found seven material local issues. Read task-7-review.md in full; its R1–R7 findings are the authoritative verbatim fix list. Address all seven and only changes required for them, updating the implementation report to accurately describe actual guarantees and tests. Preserve the review report and reviewer reproducer unchanged. No deployment, email, live provider calls, private archive access, staging, commit, push, broad cleanup or other agents. Preserve localhost8787 and all SQL0001–0006.

The controller froze the reviewed Phase7 state as tree 2f0b58c6192bb2254fdd55eb26a015ea33d1aa83 using a separate temporary index. The actual index is still reviewed Phase1–6 tree 64f93b5d350664320a8c1f3d0c977f337b1cd7d4 and HEAD95799ea261871402be2598f5aea3d4109f6065f2. Never alter the actual index. Report every changed/new path; scoped re-review will compare against this frozen Phase7 tree.

Use maintained OAuth mechanisms and installed provider source for lifecycle fixes. R1 must close pending-code and issuance/disconnect races as well as ordinary token deletion, with explicit later re-consent unable to revive older credentials. R2 includes management routes outside the custom Account path. R3 includes signed-out login and fresh verified-signup continuation, retaining safe callback/Origin checks. For R4 provide independent MCP capability release controls defaulting off in every environment/config/type/readiness surface, with actual-transport assertions. For R5 do not invoke mutating migration helpers in diagnostics; synthetic evidence must show inspection leaves a recognized existing store with pending schema unchanged and does not create a nonexistent store. Never inspect protected historical stores. R6 assertions must demonstrate the exact named credentials/replay/expiry/client/session/consent/queued-generation contracts and at least one real installed SDK client. State the deliberate refresh retry policy honestly. R7 fixed-query aggregates and finite rejection logs must be truthful and redacted.

First send a concise design note for the R1 issuance boundary and R3 continuation approach once library evidence establishes it; continue authorized work without waiting for controller approval. If the maintained provider cannot safely implement a required contract, report the concrete limitation rather than inventing security properties.

Run focused red/green tests for the confirmed defects and affected native/browser suites, required typechecks/lint/dry builds. Expand only when changes or failures justify it. Keep dependency upgrades out unless a finding requires them. Record exact final counts and meaningful assertions, separate local and external acceptance, and write task-7-fix1-report.md. Update task-7-report.md claims as necessary, then stop for scoped re-review. Do not start Phase8 implementation.
