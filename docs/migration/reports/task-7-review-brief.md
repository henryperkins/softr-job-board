# Task 7 independent review

Review Phase 7's local implementation for specification compliance and code quality. Read task-7-brief.md for exact requirements, task-7-continuation-brief.md for the user's latest authorization/context, task-7-report.md for implementation and verification claims, and task-7-review.patch for the complete review package. This is one task-scoped gate; whole-branch integration review follows separately. Worktree is C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/outputs/softr-job-board.

Base is the frozen reviewed Phase 1–6 index tree 64f93b5d350664320a8c1f3d0c977f337b1cd7d4, whose parent repository commit is 95799ea261871402be2598f5aea3d4109f6065f2. The working tree is uncommitted; the controller's package includes all identified new Phase 7 files and modified files without changing the real index. Read the patch once; read a changed file again only when necessary context is missing, and name the reason. Inspect unchanged code only for a concrete named shared-interface risk. Do not rederive the patch via broad Git commands.

Binding constraints: maintained Better Auth email/password with fresh accounts; no Auth0, legacy claiming or email-based ownership linking. Same immutable actor and owner-aware services across UI/API/MCP; strict inputs cannot override owner/model/URL authority. Independent read/write/generation/review permissions and release gates. Opaque tokens require the provider's maintained active-token validation, exact issuer/resource, expiry, current verified user and immediate durable token/grant revocation. Consent must be explicit, authoritative and origin protected; code/PKCE/redirect/client/refresh replay contracts remain intact. No cookie-only MCP, no arbitrary client metadata fetch, no SSRF surface, no workspace-admin tools. Prior SQL0001–0006 remains immutable; 0007 is additive. Logs exclude tokens, query strings, profile/resume/provider content and raw exception strings. Operators use trusted CLI, no forgeable user-admin endpoint. Native email remains restricted to no-reply@auth.lakefrontdev.com and disabled remotely. Real source/staging/cutover evidence cannot be replaced by synthetic tests.

Judge the report's claims against the patch, including current scripts/docs/CI, meaningful browser and native test coverage, Windows operator-CLI behavior, strict missing-evidence rejection, and truthful pre-write vs post-write release documentation. Do not assume an asserted rationale makes code correct. Tests were run by the implementer; rerun only a focused reproducer for a concrete unresolved doubt, not entire suites to repeat the report. Use current primary docs/installed library source as needed for a named authentication-library contract; report what was checked.

Read-only on code, tests, index and Git state. No implementation edits, commits, stage, cloud mutation, real email, provider requests, private archive reads, or other subagents. A bounded ignored synthetic reproducer is allowed if it adds evidence; keep any credentials synthetic and do not touch localhost8787. Write your report only to task-7-review.md in this plan directory.

Return two explicit verdicts: spec compliant/issues found (with any cannot-verify-from-diff items) and quality approved/needs fixes. List actionable material findings with severity, exact file/line, trigger and impact; distinguish code defects, missing evidence, and future deployment gates. Cite concrete strengths. A future external gate is not automatically a code defect, but missing required local functionality or false readiness claims are. Send a concise final verdict and report path to the controller. Do not conceal unresolved requirements behind an overall PASS.
