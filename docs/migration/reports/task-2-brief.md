# Task 2: Worker/identity/data foundation
Read this first. Worktree: C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/outputs/softr-job-board. Implement new foundation only. Parent separately owns scripts/migration/**, tests/unit/migration.test.mjs, fixtures/migration/**, and evidence docs; do not edit those. Do not spawn subagents. No external writes/deployments, email sends, commits or pushes. Use an isolated local Worker/D1 test runtime. Read docs/architecture/decisions/0001-cloudflare-migration.md and public baseline as context if available; baseline browser gate remains pending and does not block independent code preparation.

User explicitly: regular email/password, no Auth0. Use Better Auth 1.7.4 with native D1 support (verify from installed source/current official docs); no custom password crypto. Maintained library handles password hashing, reset/verification tokens, secure HttpOnly cookie sessions, logout/revocation, trusted origins and CSRF. Use database-backed rate limiting. Email verification required. Mail uses Cloudflare Email Sending binding (current https://developers.cloudflare.com/email-service/) when explicitly configured, with MAIL_MODE=disabled default and no raw token/link logging. Email subdomain is not yet configured, so signup/recovery should expose unavailability honestly, not succeed while dropping mail. Set basePath /auth; keep /login,/sign-up,/forgot-password,/reset-password shell pages for later frontend. Generate Env using wrangler types; add secret types via generated configuration/declared interface extension where appropriate, never infrastructure tokens.

Create pnpm workspace (apps/web planned, workers/app, workers/jobs planned, packages/domain, packages/data), root package.json type=module, strict TypeScript, pinned dependencies/lockfile, CI checks without secrets. Available Node v24.15.0 and pnpm.cmd 10.34.5. Npm latest already verified: wrangler 4.131.0, @cloudflare/vitest-pool-workers 0.22.0 (requires vitest ^4.1.0), better-auth 1.7.4, hono 4.13.7, zod 4.6.2, Vite 8.3.0, React 19.3.0. Pin exact versions that you actually install/test. Fetch current Workers best practices and use local types/schema. Choose Vitest Worker pool or Miniflare integration harness, but tests must exercise real D1 constraints and actual auth/session handlers, not mocked repositories. Use meaningful TDD and record red/green evidence in report.

Implement app Worker + D1 schema + shared services/HTTP validation. App Worker configuration staged/non-production by default, workers_dev=false and preview_urls=false until protected host configured; no live resources yet. Jsonc compatibility_date 2026-09-11, nodejs_compat, redacted observability. D1 names job-board-db-local/staging/production and R2 job-board-private-local/staging/production with unprovisioned IDs explicitly placeholders, deployment blocked until supplied. Run app routes before SPA fallback; unknown /api, /auth, /mcp, /.well-known must not return HTML 200. A minimal placeholder asset is okay for foundation, do not build full frontend.

Separate auth tables (auth_user, auth_session, auth_account, auth_verification, auth_rate_limit) from business users. Immutable actor identity mapping via auth_identities issuer=email-password, subject=auth_user.id to business users.id. Runtime never joins private data based on email. A new verified auth account may receive a new empty business user via an atomic/idempotent initializer, never silently claim imported legacy Users data. Legacy auth claiming remains explicit and pending. Normal API users only; operator privilege separate, do not trust imported Role or request-supplied userId. Request schemas reject unknown ownership fields.

Business schema should support users/auth_identities; multiple candidate_profiles and immutable profile_versions with explicit active choice; jobs + job_versions/job_sources; saved_jobs; draft_applications + draft_versions; attachments with owner/object key/media/size/checksum/status; generation_requests, ingestion_runs, outbox; audit_events; legacy_id_map/import_manifests. Preserve source-system/business timestamps separately, legacy IDs, unknown values as null. Use composite unique constraints + foreign keys so draft/saved/profile/attachment ownership cannot contradict. Batch related D1 writes transactionally. Record API mutations as audit metadata without content or secrets. Execution states separate from business labels. Legacy source fields not mapped yet will be preserved by parent migration pipeline; avoid destructive migrations.

Foundation HTTP contract for later UI:
GET /api/session returns {user:{id,email,name},flags:{generationEnabled,mcpEnabled,writesEnabled}} or 401.
GET /api/dashboard returns owned saved items/counts using real queries.
GET /api/jobs search q, remote, employment, seniority, bounded limit/cursor stable order; GET /api/jobs/:id resolves internal or legacy ID; job details must have safe HTTP(S) source URL.
GET/POST /api/profiles and GET/PATCH /api/profiles/:id; mutations explicit expectedRevision when editing; data persists and old profile versions retained; POST /api/profiles/:id/activate.
GET/POST /api/saved-jobs; POST body {jobId} idempotent per user/job; GET/PATCH /api/saved-jobs/:id supports expectedRevision, notes, priority, business status, outcomeNotes, submissionUrl. Saved labels Saved, Draft Requested, Draft Ready, Submitted, Rejected, Offer, Archived; explicit valid server transitions and reconcile draft summary.
GET /api/drafts/:id and PATCH with expectedRevision/coverLetter/shortAnswers/reviewerNotes; approval explicit POST /api/drafts/:id/approve with expectedRevision. Draft labels In Progress, Ready for Review, Approved, Needs Edits. Concurrency checks prevent overwriting edits/approved versions.
GET /api/attachments/:id/download checks owned row + available/scanned status and reads private R2 only; Content-Disposition attachment and no-store/nosniff, don't inline untrusted HTML.
Generation and MCP endpoints may return explicit disabled/not-ready states; do not advertise implementation that is missing. Writes/runtime flags independent and disabled remote defaults. Local tests can enable flags explicitly without production auth bypasses. No automatic application submission.

Required focused tests: unauthenticated rejection; two genuinely authenticated synthetic users cannot list/read/update each other's profiles/saves/drafts/files; forged owner fields rejected; CSRF/origin failure; logout/revoked/expired session rejected; duplicate save idempotency; revision conflicts; transaction rollback and composite FK ownership constraints; unknown API never SPA; safe job URLs; writes disabled. Include actual BetterAuth sign-in with synthetic verified accounts in Worker runtime and meaningful cookie checks. Synthetic fixtures only; never read parent private-source for tests.

Expose npm scripts lint, typecheck, test:unit (include existing node migration tests without altering them), test:integration, test:security, build, dev, cf:types. Do not create fake passing scripts for unimplemented e2e or deployments. Document actual API response shapes for next UI task in your report.

Write complete implementation report to .superpowers/sdd/softr-cloudflare-migration-plan-2026-09-11/task-2-report.md: files, commands/outcomes, exported interfaces/schema details for parent migration tools, limitations and exact auth/mail/deployment prerequisites. Return status, focused test summary, concerns. Do not mark plan gate passed without deployed evidence. Parent will review the diff and integrate docs.

