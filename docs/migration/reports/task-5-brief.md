# Task 5: durable source ingestion and local shadow comparison

Worktree: C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/outputs/softr-job-board. User asks execute attached migration plan, changed Auth0 to email/password. Task1-3 locally reviewed; Task4 review must pass before dispatch. Read this brief and current foundation interfaces; no need to reconstruct entire plan. No subagents, commits, pushes, deployment, provider paid calls, actual production mutations or source workflow execution. Do not read work/private-source. Parent owns migration/identity evidence. You own new background Worker/provider/domain/ingestion persistence, additive0005_ingestion.sql, focused tests/fixtures/local shadow tooling/docs and necessary root config integrations. Do not modify applied0001-0004SQL; coordinate shared app/domain edits. Both Worker configs must stay disabled/unprovisioned for remote use.

The target is one background Worker with IngestSourceWorkflow, scheduled dispatcher, and later GenerateDraftWorkflow. Use plain Cloudflare Workflows; no Queues, Durable Object/Agents framework, Vectorize or new job provider. Follow workers-best-practices and wrangler skills before their operations. Strict typing/build both Workers. Existing root TypeScript includes only workers/app; extend appropriately without conflicting global generated Env. Generate background binding types. Native D1 real Worker runtime integration tests should exercise persistence, not only mocked DB chains.

## Source evidence

Existing source normalizer (credential-free, untrusted reference code) is outside repo at C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/work/source-providers/legacy-normalizer.js. Read it; do not execute arbitrary source code. Its defects: greenhouse:<id> lacks board; Greenhouse closure combines boards; Fantastic/Jobven rolling feeds; only two Jobven pages; trim6000 descriptions; salary annualized without currency/unit; Jobven title+company dedupe; useful remote/seniority/tag heuristics but guesses unlabeled; .slice(0,40) create/update queues lose backlog; limited content refresh. Capture representative synthetic legacy normalization inputs/results as reviewed fixtures explaining the deliberate differences. Preserve unknowns as unknown and inferred values with provenance.

Public raw Greenhouse responses already captured once (no need to refetch): C:/Users/htper/Documents/Codex/2026-09-11/files-mentioned-by-the-user-softr/work/source-providers/greenhouse-20260911/{anthropic,stripe,figma}.json and manifest.json. All content=true, capture2026-09-11T12:35:10Z, Anthropic594/8.6MB,Stripe626/4.9MB,Figma156/1.7MB; declared meta.total and unique IDs agree. Large outputs must go to R2, never an 8MB Workflow step result. Use tiny synthetic cases in public Git fixtures; raw archived snapshots stay outside repo. Implement CLI local replay that accepts these paths and writes only a new protected local shadow DB/R2/report outside repo, so parent can reconcile1376records using exact captured snapshots and repeat for0changes. Never use remote bindings or captured credentials.

Allowlisted sources independent logical keys:
- greenhouse:anthropic,stripe,figma — GET https://boards-api.greenhouse.io/v1/boards/{board}/jobs?content=true. Official https://docs.greenhouse.io/job-board.html: response all posts + meta.total. Validate complete count/uniqueIDs/schema. Keep board in source identity. Full snapshot eligible for closure ONLY that board after successful complete normalization/import. job-specific absolute_url; retain complete description/provenance/requisition/offices/locations/etc.
- fantastic:active-ats — https://data.fantastic.jobs/v1/active-ats using configured secret binding Authorization, never literals/logs. Existing params time_frame=24h,limit=50,description_format=text,include_basic_organization_details=true,ai_taxonomies_a=Software, Technology, Data & Analytics (read exact sanitized config if needed; do not inspect raw credentials). Docs https://developer.fantastic.jobs/documentation/endpoints/new-jobs and /api/new-jobs. 24h feed uses offset until short page, limit1..1000, credits1/job; rolling even after complete pages, never closure-by-absence. Fields/AI provenance/currency/period retained. Missing credentials fail only this source, no paid API calls in this task.
- jobven:public-jobs — https://api.jobven.com/v1/public/jobs, secret X-API-Key, limit25, descriptionFormat=plain, configured skills Python,JavaScript,TypeScript,React,Node.js,AWS,Docker,Kubernetes,SQL,Go,Java,Machine Learning. Docs https://jobven.com/docs/reference/jobs and /reference/data-types. Cursor meta.nextCursor+hasMore until false, no two-page cap. Reject repeated/missing cursor and contradictory metadata. Preserve locations/companies and salary currency/period including unknown/optionalfields. Rolling/filtered endpoint never closure-by-absence; explicit closed/expired source status can be handled source-specifically. Freepage25 paidquota unknown; do not call live.

Use current official docs/local installed types to resolve API details; above context is verified but exact runtime/schema must compile. Bounded fetch timeout/response bytes/pages/retries; exhaust a bound -> incomplete with durable cursor/backlog, never silently succeed/truncate. Save each fetched raw page with immutable checksum and capture metadata to private R2. Schema validation failures and partial captures retain failure state and counts. No credentials/rawheaders in logs.

## Durable behavior

Each source/date run independent Workflow, acquired from D1 unique(source,chicago_date). Hourly UTC cron dispatcher evaluates America/Chicago and schedules current date at or after07:00 and before local midnight (no automatic backfill earlier dates). Exercise both2026 DST transitions. Source failure cannot block another source scheduling. Keep date/run idempotency in D1 indefinitely; Workflow retention is not uniqueness. D1 run + outbox insertion transactional, recoverable create/ambiguous response using same deterministic Workflow ID (<100chars), get/status/retry safely, no new random duplicate when response unknown. Track attempts/available_at/errors without unbounded retries. Background schedule/write flags independent and false by default; tests can explicitly enable only local bindings. Live credentials (Fantastic/Jobven) are secret bindings and not in app Worker.

Persist raw page progress and normalized record backlog; process every record with bounded batches, updating progress atomically with each chunk or record. Workflow replay/resume must not duplicate versions/audits/content. Keep source observation freshness based on successful complete capture, not scheduler invocation. Store counts created/updated/unchanged/closed/rejected, cursor/complete flags, timings/failure reason and last successful source observation. Source-owned content refresh includes descriptions/tags/locations/salary/provenance, not only title/location; saved notes/drafts/profile content remain untouched. Job content versions immutable; changes and audit rows transactionally linked. Job_versions current content JSON has {core,...additionalfields}; align with app getters.

Upsert by board/provider/external identity. Existing imported jobs have job_sources source_system='softr-legacy', source_key=<table>:<record>, source_url current original, and job_versions includes externalId/source fields. Preserve legacy URLs/IDs. Do not merge by title/company. Automatic cross-source reuse is limited to unambiguous exact job-specific canonical HTTPS URL evidence (preserve meaningful query IDs; do not strip requisitions); ambiguous or generic URLs retain separate records and create a review disposition. Explain any conservative nonmerge. Source ownership of a reused shared job must be stable/deterministic; daily alternating providers must not fight over the canonical content. Every alias retained. Provider disappearance never deletes owned saved/draft relationships.

Closure only after board complete, all pages/rows validated, source import drained, and scoped to rows observed by that exact board. Zero/implausible removals require a fail-closed circuit breaker; choose conservative default threshold as configuration, record pending review instead of mass-closing. A rolling Fantastic/Jobven run or partial/429/malformed/timeout run can never perform closure. Test empty board, rejected rows and >threshold removal; do not interpret failure as zero jobs. Existing saved references remain readable when listing closes.

Operator controls: no normal-user retry/admin endpoint. Provide trusted local/operator CLI/service functions with explicit source/run/retry state validation and redacted inspection. Public background fetch should return404/deny; operator Cloudflare tooling remains outside application identity. If an authenticated operator HTTP surface requires Access JWT config not available, use documented tooling path rather than fake auth header. Explicit rerun should reuse original run/backlog or auditable retry revision; never override successful date uniqueness silently.

## Validation and delivery

Meaningful tests must cover duplicate run/create response ambiguity; per-source timeout/429/500/malformedbody independence; complete/partial pagination including missing/repeatedcursor; resumable backlog>40; salarycurrency/unit unknowns and no6000charloss; distinct same-title differentlocations; content-only refresh; safe closures/rollingabsence/circuitbreaker; Chicago clock/DST; interrupted Workflow replay; redacted logs; unchangedrepeat. Use fetch fixtures, real D1 integration where valuable, and actual Cloudflare Workflow wrapper typecheck/drybuild. No tests whose only assertion repeats implementation. Run focused checks once then covering regressions after fixes.

Local shadow replay should produce aggregate comparison report with sourcecounts/completeness/actioncounts/normalization differences/sourcehashes and pending provider entitlement gates. It may report fixture-based coverage for paid providers but must not label their live acceptance passed. README/runbook show exact commands, disabled default flags and operator recovery, docs cite relevant official APIs.

Write task-5-report.md in this scratch folder including tests/commands/output, everychangedfile, interfaces for next generationtask (outboxdispatcher/Workflow bindings), localshadowcommand, knownlimits. Stop editing when done. Parent stages and independent reviewer checks bounded task diff. Generation and MCP not in this task.

Parent local aggregate comparison (2026-09-11T13:03:20Z) of current exported295jobs and capturedpublicGreenhouse snapshots:182legacy greenhouse:<id> rows;174match exactly one board by BOTH externalnumericID and exactabsolute_url (Stripe93,Anthropic71,Figma10);8absent;0ID-only,0URL-only,0ambiguous. Other source IDs comprise44ats,42jobven,27other. This is evidence for conservative adoption design, not closure/deployment authorization. Aggregate report only at work/source-providers/greenhouse-legacy-comparison.json; do not read private exportedrows. Keep unmatched8sourcejobs pending; absence alone from a reference comparison is not a cutover closure approval.
