# Phase 7 scoped fix round 2

Read `task-7-fix1-review.md` in full. Its four actionable findings under R1, R5 and R7 are the complete fix scope. R2, R3, R4 and R6 are closed. Preserve the original review reports, reviewer reproducers and frozen evidence.

The user stopped ancillary staging preparation; Phase 7 defect fixes remain authorized. Do not start Phase 8, provision or deploy anything, send mail, call live providers, inspect private historical archives, alter localhost:8787, change the real Git index, commit, push, clean, or delegate. Preserve SQL 0001–0006 and unrelated work. Fix against the stable first-round tree `4cf886f5901bed4c2e581b35b4d949c384cfb4e9`. Real index remains `64f93b5d350664320a8c1f3d0c977f337b1cd7d4`; HEAD remains `95799ea261871402be2598f5aea3d4109f6065f2`.

## Required corrections

1. R1: Shared `auth_verification` contains plain user IDs for password reset. Both new INSERT triggers and the disconnect DELETE must handle non-JSON values safely; SQL predicate ordering is not a safety guarantee. A reset request currently fails with `malformed JSON`, and a pre-existing reset record can abort disconnect. Preserve the epoch design and add ordinary verification/reset plus pending-reset/disconnect regression cases. Controller evidence is in `task-7-fix1-controller-checks.md`: the unchanged existing native suite produced 29 pass / 2 fail, with existing-account reset 500 instead of 202. Rerun the two failing assertions unchanged after the correction.
2. R5: Opening the original SQLite file read-only still creates WAL/SHM sidecars. Inspection must leave the entire original store unchanged and correctly include committed WAL content. Cover a recognized, cleanly closed WAL-mode store and a genuinely pending schema. Compare the complete original directory file set, including SHM, and file contents before/after. Do not exclude sidecars or weaken the immutability assertion. Use synthetic stores only.
3. R7: Installed SDK 1.30 has no `Mcp-Name` header. Its schema rejection occurs before the callback and currently produces only `mcp_call_completed`. Use bounded, validated protocol information to observe refusals for every supported transport revision without logging raw arguments/tokens. Preserve finite reason/tool allowlists. Turn the demonstrated real-SDK unknown-field refusal into a regression assertion.
4. R7: Diagnostics currently collapses `last_error` to `present`. Aggregate the existing allowlisted `dispatch_unconfirmed` and `dispatch_lease_exhausted` reasons distinctly with a finite fallback for unexpected values. Verify both synthetic states and redaction of an unexpected value.

Run focused red/green checks for these defects and the affected existing security/native suite. Run appropriate typecheck/lint/format checks for changed code; avoid unrelated broad retesting, audits, dependency upgrades or staging preparation. Update implementation documentation only where these findings require it. Correct blanket completion/immutability/logging claims until evidence supports them.

Write `task-7-fix2-report.md` with every changed/new repository path, concrete implementation guarantees, exact commands/results and remaining external limitations. Update `task-7-report.md` to the final supported local claims. Then stop editing and notify the controller that the candidate is stable for scoped re-review.
