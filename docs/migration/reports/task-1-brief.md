# Task 1: migration contracts

Read this first; this is the bounded requirements. Work only in docs/migration/parity-matrix.md, docs/architecture/decisions/0001-cloudflare-migration.md, docs/migration/identity-activation.md, docs/migration/cutover.md, docs/migration/rollback.md. Preserve existing source-era docs. Do not change any other files or execute external writes. Do not spawn subagents.

User asked to execute the attached migration plan. Source is public repository henryperkins/softr-job-board, baseline 95799ea261871402be2598f5aea3d4109f6065f2. User explicitly changed authentication: 'Let's not use auth0, just regular email / password auth'. Use Better Auth native email/password on D1 (implementation pending), immutable internal user IDs, verified one-time activation/claiming with ambiguity rejection; emails are not runtime owner keys. Do not claim password hashes available or currently imported. Email delivery provider must be configured and verified. Never auto-activate source NOT_INVITED accounts or auto-link solely on asserted email.

Architecture: React/Vite app Worker with authenticated API; independent background Worker with Workflows; dedicated D1/private R2 for staging and production. No generic SQL MCP, automatic application submissions, public job catalog, matching claims, Queues or Vectorize. Five business routes are private. Ownership same in UI/API/files/workflows/MCP. Multiple profiles allowed; explicit active profile/version. Profile writes actually persist. Canonical listing/detail bind real IDs and preserve legacy query URLs. Source URLs allow HTTP(S) only. Drafts preserved as legacy output, editing/approval use revisions, generation independent and off until assessed. MCP remains in scope until user explicitly excludes; disabled until ready is a blocker, not completed parity. Source ingestion is at/after 07:00 America/Chicago with unique source/date keys; snapshot-only closure, no closure from partial fetch or rolling feeds. D1 outbox handles ambiguous dispatch.

Parity matrix must cover dashboard '/', '/jobs', '/job-details?recordId=', '/profile', '/saved-job-details?recordId=', '/sign-up', '/login', '/account', '/onboarding', '/forgot-password', '/reset-password', '/link-expired', app MCP, workflows, attachments. Each row: preserve/repair/complete/defer/exclude, target behavior, exact test, evidence status/owner. Fresh builder and data evidence is being collected by controller; don't invent pass evidence. Known current: published 2026-09-07T05:30:55, builder updated 2026-09-11T10:07:24 (no timezone inferred); database counts 6 Users, 9 profiles, 295 jobs, 30 saved, 9 drafts. Admin roster six records: one ACTIVATED, five NOT_INVITED, all active=true (does not imply login working). Published unauthenticated /jobs redirects to /login?next-page=/jobs. Two controlled user browser acceptance pending. Current full native bindings still being investigated. Provider entitlements and email delivery pending. Unknowns must be explicit test + owner, never silently accepted.

Cutover: separate explicit deployment action after gates. Maintenance/source write freeze including Softr subdomain and MCP plus ingestion. Final complete export and byte reconciliation. Remove/reconcile old apex/www routes to lakefrontdev-origin-proxy before enabling proxied DNS. www currently CNAME to apex; apex A 35.158.87.123; both DNS-only. Preserve other DNS/mail records and proxy worker until dependencies checked. Validate reads first before target writes/schedule. Exact git commit, Worker versions, migrations, manifest and recovery bookmark required. Pre-write rollback validated effective old routing; after writes roll forward/application rollback retaining D1 unless target delta replay to Softr rehearsed. No lost accepted writes; D1 restore distinct destructive action; R2 immutable versions.

Keep docs concrete and concise. No raw user records/emails/attachment URLs, workflow graphs or credentials. Add links to plan's official references where appropriate, and internally reference forthcoming evidence-manifest.md without claiming it exists until controller adds. Write complete report to task-1-report.md in this same directory; final return status, touched files, focused verification and concerns only. Do not commit/push; controller handles scoped publication after review.

