# Phase7 fix round1 re-review

Review only the fixes for R1–R7 in task-7-review.md and integration risks introduced by those fixes. Read that report, task-7-fix1-brief.md, task-7-fix1-report.md, the corrected task-7-report.md, and the controller's frozen fix patch/manifest. Baseline is the original reviewed Phase7 tree2f0b58c6192bb2254fdd55eb26a015ea33d1aa83. Candidate tree and patch hash will accompany dispatch. Real index must remain Phase1–6 tree64f93b5d350664320a8c1f3d0c977f337b1cd7d4; HEAD95799ea261871402be2598f5aea3d4109f6065f2.

Read the patch once. Inspect current or unchanged files only to answer a named missing-context/shared-interface concern. Do not restart the original Phase7 review or perform a broad audit. Report whether each original finding is closed, with the concrete code/assertions establishing it. A passing count alone is insufficient, but do not rerun passing broad suites without a concrete unresolved concern.

For R1, judge the durable issuance/revocation invariant, not just existing-token deletion. Installed provider source consumes auth_verification before awaited checks/token insertion, and persists new codes after prior consent checks. Confirm server-derived intent/grant version binds the exact user/client/session/authorization continuation, and database/resource boundaries reject consumed-code→late-token insertion, prior-consent-read→late-code insertion, and old-intent-after-reconsent. Later explicit consent must not revive old credentials. Review compatibility with non-OAuth verification/session/password-reset lifecycle where the new triggers/hooks touch that shared surface. R2 must close alternate consent management routes with Origin/owner checks intact. R3 must preserve signed-out login plus fresh signup/verification through maintained validation and safe redirects.

For R4 check all capability controls/defaults/config/type/readiness surfaces and actual-transport negative tests. For R5 confirm diagnostics cannot create/change original stores, metadata/schema/ledger or DB/WAL state, including pending migrations; all reproductions must be synthetic. For R6 check real credentials, explicit refresh retry policy, expiry/cross-client/user-session/consent cases, successful queued generation and actual installed SDK transport. For R7 confirm bounded truthful aggregates, drift reporting and finite redacted rejection logs. Validate report claims against actual assertions and local interfaces.

No code/test/index/Git edits, private source archive access, real mail/provider calls, cloud operations, localhost8787 changes, or other agents. A bounded ignored synthetic reproducer for a concrete doubt is allowed. Write only task-7-fix1-review.md with explicit spec and quality verdicts, per-finding closure, and any actionable remaining/newly introduced defect with exact path/line/trigger/impact. Local acceptance is separate from deployed acceptance. Latest user stopped ancillary staging preparation; do not propose more staging setup as a condition for accepting these local fixes.
