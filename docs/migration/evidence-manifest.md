# Migration execution evidence

Collected September 11, 2026 against source commit `95799ea261871402be2598f5aea3d4109f6065f2`. This is a rehearsal; Softr remains writable, so this is not the final cutover manifest.

## Current evidence

- [Machine-readable baseline](baseline-2026-09-11.json) records current app timestamps, table/schema totals, administrative auth roster aggregates, statuses, and relationship checks.
- [Source schema](../../fixtures/migration/source-schema.json) contains all 90 application fields and configured options/relations, with no source user rows.
- Builder and published application remain distinct: last published `2026-09-07T05:30:55`, builder updated `2026-09-11T10:07:24`. Softr omits timestamp zones here.
- Builder `list_application_users` returned all six administrative auth entries (`hasMore=false`): one ACTIVATED, five NOT_INVITED, all active flags true. All six email values correlate uniquely with a Users row; that correlation does not activate or authorize an account.
- All five business page VIEW permissions are LOGGED_IN_USERS. A real unauthenticated browser request to `/jobs` reached `/login?next-page=/jobs`.
- Paginated builder export captured 349 records with IDs and system timestamps: six Users, nine profiles, 295 jobs, 30 saved jobs, nine drafts. Schema counts: 14, 19, 33, 12, 12 respectively.
- Structural relationship checks found no missing/dangling owners, broken saved-job/job links, draft-owner mismatches, duplicate user/job saves, or archive-checkbox/status conflicts. Three owners have two profiles and three have one.
- All 26 attachment references downloaded to a protected local directory: 451,499 bytes summed by reference, six unique byte streams. Every declared size and media signature matched. SHA-256 hashes were recorded. The local D1/R2 rehearsal verified all 26 immutable owner-specific objects against actual bytes. The subsequent source-file scan is recorded below; deployed target and quarantine-release acceptance remain pending.
- The repeatable exporter and [local rehearsal](rehearsal.md) reconciled all 349 business records, six administrative roster entries, 90 fields, relationships and immutable history. A final repeat changed zero of 375 mapped roots and copied zero of 26 objects. It reports `structurallyReconciled=true` and `releaseReady=false`; no target identities were activated.
- Independent migration review approved schema-change rejection, full immutable snapshot history, attachment metadata hashing and rejection of stale A-to-B-to-A archive replay. Focused tests exercise target drift, partial import recovery, deletion dispositions, missing/corrupt bytes, and repeatability against the local Worker runtime.
- Local application verification passed 30 native Worker integration checks (17 security, six data, seven user-flow checks), eight core browser flows, a separate five-route/two-viewport rendering sweep and three browser regressions from independent review. Synthetic fresh accounts exercised persisted profiles, saved jobs, draft review, account/session changes and owner isolation. The local acceptance details and their test sources are linked in the [parity matrix](parity-matrix.md#local-execution-results). No delivered verification/reset email or controlled Softr mutation was credited.
- A quiescent local recovery copy was captured at `2026-09-11T13:41:28Z`: all 41 state files (14,705,787 bytes) matched their source hashes, all five SQLite stores passed integrity checks, and the application database had no foreign-key errors. Its 349 business records, 26 attachments and zero target identity mappings were retained. At `13:46:18Z`, a read-only check of the restored application data and all referenced R2 bytes reconciled with no issues. This establishes local recovery evidence; deployed Cloudflare restore remains unverified.
- Public Greenhouse snapshots were captured read-only at `2026-09-11T12:35:10Z`: Anthropic 594, Stripe 626, Figma 156 postings. Each declared count matched the unique IDs returned. The replacement ingestion engine imported all 1,376 into a persistent local shadow and, after reopening it, re-normalized the same bytes with 1,376 unchanged jobs and zero new versions. No paid Fantastic Jobs or Jobven request was made; those providers have fixture coverage only.
- A second controller shadow at `2026-09-11T13:55:26Z` started with only the 295 archived shared jobs, their versions and source aliases. It reused exactly 174 existing job IDs (Anthropic 71, Stripe 93, Figma 10), created 1,202 new jobs, retained all 295 legacy IDs, and closed/rejected zero rows. The resulting 1,497-job catalog had no foreign-key errors. It contained zero users, identities, saves, drafts or attachments. This matches the prior read-only ID/URL comparison and does not authorize closure of the eight unmatched historical Greenhouse rows.
- Independent ingestion review identified and verified fixes for generic listing-URL conflation and stale closure during overlapping runs. All 25 runtime tests pass; the reviewer independently reran the four new regressions. The corrected harness (`51cd605c5828a1874bd2e9dd10c9915cd863ff6d80bce616af647d1f56c3457d`) reopened both protected shadows and processed the original captured bytes with zero creates, updates, closures, rejections or new versions. Logical next-date labels were synthetic; the original source capture time was retained.
- Current ingestion is enabled version 2, draft 0, with schedule `0 0 7 * * 1-7` in America/Chicago. Draft generation is disabled version 1, draft counter 10.
- The application-resource inventory contained no `job-board-*` resources. Website apex A and www CNAME were DNS-only, with stored legacy origin-proxy routes. No application deployment or website-routing change was performed.
- Sender refresh at `2026-09-11T14:33:39Z`: the user-selected `auth.lakefrontdev.com` sending domain already existed with ID `dbf06d43bb0e4626a0383fcacbb48d87`, enabled, with six DNS records ready and zero errors. The controller changed only `preview_enabled` to false, then verified it by GET; suppressed-recipient behavior remained false. The account quota was 1,000/day, sent zero. This is sender/DNS configuration evidence, not Worker binding deployment or mailbox delivery proof.
- At `2026-09-11T14:52:03.997Z`–`14:52:16.535Z`, a local ClamAV 1.5.4 scan inspected the six unique archived files and returned zero detections/errors with every original SHA-256 unchanged. The portable package's Talos signature and all three official signature databases were verified; the daily database was version 28120, built September 11. Harmless custom-signature positive/negative controls and a missing-file error control returned the expected exit codes 1/0/2. The scan enforced type/parser, encryption and resource-limit alerts, current database age and private temporary storage. No private file was uploaded, system antivirus setting changed, D1/R2 record modified or attachment released. Protected `scan-rehearsal-20260911/scan-report.json` contains detailed provenance; no-detection evidence is not a guarantee that content is harmless.
- Phase 6 generation completed independent review after two scoped fixes: first-generation queued drafts can save edits, and profiles with no evidence eligible for the 6,000-character whole-field contract fail before acceptance/quota/provider effects. The final controller run at `09:54:48 America/Chicago` passed all 37 generation tests (10.21 seconds). The reviewer independently passed four boundary/race tests and eight actual-source UI-condition assertions; the new first-generation browser regression also passed. Generation remains off. Native structured-output fixtures do not establish live Anthropic account access or human evaluation.

## Protected evidence

Raw records, administrative roster, expiring file URLs and downloaded bytes are outside the repository under the task's `work/private-source/` directory. Its inherited Windows ACL was removed and access granted to the current Windows user. Do not add that directory to Git, public CI artifacts, logs, or this documentation. Public reports include aggregate evidence only.

## Open acceptance evidence

| Evidence                                  | Owner                       | Concrete completion test                                                                                                                                                                                |
| ----------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two controlled target identities          | Implementer + project owner | Fresh target accounts pass ownership isolation and live verification/reset delivery. Source account migration is excluded.                                                                              |
| Native action bindings/defaults/filtering | Migration operator          | Inspect builder native block settings and perform reversible owned test mutations in the source browser; record before/reload/second-session results. The current get_block tool exposes metadata only. |
| Fresh signup + password recovery delivery | Implementer + project owner | Email/password replacement uses maintained auth; test verification/recovery with a configured sender, single-use/expired links, rate limits and revoked sessions. No Auth0 or source account claiming.  |
| Source account retention                  | Recorded scope              | Source roster and business records are retained in the protected archive/rehearsal. No source account is migrated or activated.                                                                         |
| Provider rights, quotas and credentials   | Project owner + operator    | Verify current provider entitlements and secret bindings without executing source workflows or disclosing tokens.                                                                                       |
| Attachments                               | Implementer + operator      | Validate the ongoing quarantine-release process and repeat the local byte/scan/ownership evidence on private deployed R2, including another user's denied downloads.                                    |
| Staging                                   | Implementer                 | Run Worker runtime isolation suite on exact deployed commit and alternate hosts; record Worker version IDs/resource IDs.                                                                                |
| Cutover                                   | Project owner + operator    | Pass parity, fresh-account authentication, files, source shadow comparison and restore gates; then request the explicit production deployment action specified in the plan.                             |

No authenticated source mutation, email delivery, private staging import, target deployment, live generation, or production cutover is claimed by this evidence.

User correction: account migration is not required. The legacy claim implementation was removed before publication; fresh accounts receive new owner IDs and no automatic access to historical private data.

Phase 7 local implementation now includes user-scoped OAuth MCP, consent/disconnect UI, redacted diagnostics, a fail-closed post-write evidence checker/template, and focused synthetic browser acceptance. The exact Phase 7 commands and counts are recorded in its task report. Independent Phase 7 review, whole-branch review and exact deployed acceptance remain pending; MCP stays disabled by default and no production endpoint is claimed.
